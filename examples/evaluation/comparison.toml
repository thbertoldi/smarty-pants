# Deterministic CPU comparison. Production still defaults to temperature 0.2.
[model]
name = "qwen-2.5-1.5b-instruct-q4_k_m"
gpu_layers = 0
threads = 4
seed = 42
temperature = 0.0
top_p = 1.0
max_tokens = 512

[shortcuts]
enabled = false

[tray]
enabled = false
