// Daemon modules live in src/lib.rs (so `cargo test --lib` discovers them).
// main.rs depends on the lib via its package name.

fn main() {
    println!("smarty-pants-daemon placeholder; see Task 16 for real wiring");
}
